{
  "realm": "microbase",
  "enabled": true,

  "roles": {
    "realm": [
      { "name": "ADMIN" },
      { "name": "USER" },
      { "name": "PLAYER" },
      { "name": "MODERATOR" }
    ]
  },

  "clients": [
    {
      "clientId": "microbase",
      "name": "MicroBase API",
      "protocol": "openid-connect",
      "publicClient": false,
      "serviceAccountsEnabled": true,
      "directAccessGrantsEnabled": true,
      "standardFlowEnabled": true,
      "redirectUris": [
        "http://localhost:3000/*"
      ],
      "webOrigins": [
        "+"
      ],

      "protocolMappers": [
        {
          "name": "client roles",
          "protocol": "openid-connect",
          "protocolMapper": "oidc-usermodel-client-role-mapper",
          "consentRequired": false,
          "config": {
            "client.id": "microbase",
            "claim.name": "resource_access.microbase.roles",
            "jsonType.label": "String",
            "multivalued": "true"
          }
        }
      ]
    }
  ]
}